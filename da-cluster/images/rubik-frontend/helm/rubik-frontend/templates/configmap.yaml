apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rubik-frontend.fullname" . }}-nginx
  namespace: {{ include "rubik-frontend.namespace" . }}
  labels:
    {{- include "rubik-frontend.labels" . | nindent 4 }}
data:
  default.conf: |
{{ .Values.nginx.config | indent 4 }}
